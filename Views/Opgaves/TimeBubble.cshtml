@{
    var myChart = new Chart(width: 600, height: 400)
.AddTitle("Score pr elev")
.AddSeries(
chartType: "Bubble",
        name: "Bubble",
        xValue:  (List<int>)Session["chart_BubbleY"],
        yValues: (List<int>)Session["chart_BubbleSize"]
        )
.Write();
}